<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Project 1</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
      </div>
      <h5 class="mb-5">Edit your post.</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          class="p-3 outline-none w-1/3 resize-none block bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
          placeholder="What's on your mind?"
          id=""
        ><%= post.content %></textarea>
        <input
          type="submit"
          class="px-3 py-2 w-42 bg-yellow-500 rounded-md mt-2 text-sm text-black block"
          value="Update Post"
        />
      </form>
      
    </div>
  </body>
</html>
